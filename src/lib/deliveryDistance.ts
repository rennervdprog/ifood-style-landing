/**
 * @deprecated Use `@/lib/location` em vez deste módulo.
 * Mantido como shim para não quebrar imports existentes.
 */
export {
  resolveDistance,
  type DistanceResult,
  type DistanceAccuracy,
  type RouteSource,
  type ResolveDistanceInput,
} from "@/lib/location";