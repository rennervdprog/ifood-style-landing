name: OTA Release

# Publica um novo bundle OTA no bucket app-releases sempre que
# houver push em main que altere código web (src/public/index.html).
# O plugin @capgo/capacitor-updater baixa e aplica no próximo cold start.
#
# Não precisa gerar APK — o APK só muda quando código nativo mudar
# (workflow build-android.yml).

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'public/**'
      - 'index.html'
      - 'vite.config.ts'
      - 'package.json'
      - 'bun.lockb'
      - 'package-lock.json'
  workflow_dispatch:

concurrency:
  group: ota-release
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install deps
        run: npm ci --no-audit --no-fund

      - name: Build Cliente OTA
        run: npm run build
        env:
          VITE_CAPACITOR_APP_MODE: cliente

      - name: Publish Cliente OTA bundle
        env:
          OTA_APP_MODE: cliente
          EXTERNAL_SUPABASE_URL: ${{ secrets.EXTERNAL_SUPABASE_URL }}
          EXTERNAL_SUPABASE_SERVICE_KEY: ${{ secrets.EXTERNAL_SUPABASE_SERVICE_KEY }}
        run: node scripts/publish-ota.mjs

      - name: Build Parceiro OTA
        run: npm run build
        env:
          VITE_CAPACITOR_APP_MODE: parceiro

      - name: Publish Parceiro OTA bundle
        env:
          OTA_APP_MODE: parceiro
          EXTERNAL_SUPABASE_URL: ${{ secrets.EXTERNAL_SUPABASE_URL }}
          EXTERNAL_SUPABASE_SERVICE_KEY: ${{ secrets.EXTERNAL_SUPABASE_SERVICE_KEY }}
        run: node scripts/publish-ota.mjs